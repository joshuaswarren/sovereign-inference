{
  "name": "sovereign-inference",
  "private": true,
  "description": "Sovereign Inference — provider-neutral access and supply layer for open AI (SIP-AI + SIN).",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
